# feat — 功能实现节奏（Red → Green → Refactor）

每个模块：

1. **写测试（红）** — 先查看项目已有测试文件结构，遵循已有约定；若无现有测试，询问用户偏好。直接写测试，运行确认**全部失败**
2. **写实现（绿）** — 最小可通过测试的代码，直接写实现，运行确认**全部通过**
3. **重构** — 检查是否有可简化之处，重构后再次运行测试确认绿色，无需重构则跳过
4. **更新 TASK.md** — 将当前任务标记为 `[x]`，自动继续下一个任务
