# Architecture: {项目名}

> 生成时间：{date}

## 项目简介

一段话：这个项目是什么，解决什么问题，谁来使用。

## 目录结构

```
{两层目录树，每个目录/文件后加简短注释}
```

## 系统架构图

根据 findings 中识别到的实际模块和层次关系，用 Mermaid graph TD 画出分层结构：

```mermaid
graph TD
    {根据实际项目结构生成，保留真实的模块名和依赖关系}
```

## 核心数据流

根据 findings 中识别到的主要请求路径，用 Mermaid sequenceDiagram 画出 1-2 条关键流程：

```mermaid
sequenceDiagram
    {根据实际调用链生成，只画关键路径}
```

## 技术选型

| 组件 | 选型 | 理由（只写非显而易见的） |
|------|------|------------------------|

## 模块依赖关系

说明哪些模块依赖哪些模块，哪些可以独立替换。

## 关键约定

理解项目必须知道的隐式规则（不读代码看不出来的部分）。

## 功能清单

| 功能 | 简述 | 深入探索 |
|------|------|---------|
| {功能名} | 一句话说清这个功能做什么 | `/explain feature {topic}` |

> 从这里选一个感兴趣的功能，运行对应命令即可深入了解。
