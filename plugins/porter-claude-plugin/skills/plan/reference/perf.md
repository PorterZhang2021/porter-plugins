# perf — 性能优化规划

生成 `plan/perf/<branch-name>/PLAN.md`：

1. **性能问题** — 当前瓶颈在哪，如何度量（基准数据）
2. **优化方案** — 拟采取的优化手段
3. **验证方式** — 如何量化改善效果（benchmark/profiling）
4. **回归风险** — 可能影响的功能

收尾：提示下一步运行 `/execute` 或 `/task`
