# test — 测试规划

生成 `plan/test/<branch-name>/PLAN.md`：

1. **测试目标** — 补充哪个模块/场景的测试，当前覆盖缺口在哪
2. **测试类型** — 单元测试 / 集成测试 / 端到端测试
3. **用例设计** — 正常路径、边界条件、异常路径
4. **Mock 策略** — 哪些外部依赖需要 Mock，如何 Mock

收尾：提示运行 `/task` 生成测试任务清单（必须执行）
