# test — 测试任务清单格式

只有测试任务，无实现任务：

```markdown
## Task N：<模块名> 测试补充

- [ ] `<测试文件路径>`（先查看项目已有测试文件结构，遵循已有约定；若无，询问用户偏好）
  - **用例：<用例标题，描述业务行为>**
    - Given：<前置条件>
    - When：<触发操作>
    - Then：<验证结果>
  - ...
```
