# ci — CI/CD 规划

生成 `plan/ci/<branch-name>/PLAN.md`：

1. **变更内容** — 修改哪个 pipeline / workflow / 部署脚本
2. **变更原因** — 解决什么 CI 问题或新增什么自动化能力
3. **验证方式** — 如何在不破坏现有流水线的前提下验证

收尾：提示下一步运行 `/execute` 或 `/task`
