# refactor — 重构规划

生成 `plan/refactor/<branch-name>/PLAN.md`：

1. **重构目标** — 当前问题是什么，重构后期望达到什么状态
2. **影响范围** — 涉及的模块、文件、接口
3. **重构策略** — 分步拆解，每步保持测试绿色
4. **回归测试策略** — 现有测试是否覆盖，需要补充什么
5. **不变约束** — 重构前后对外行为必须一致的部分

收尾：提示运行 `/task` 生成重构任务清单（必须执行）
