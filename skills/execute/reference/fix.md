# fix — Bug 修复节奏（复现 → 修复 → 验证）

严格顺序，不可跳步：

1. **写复现测试（红）** — 先查看项目已有测试文件结构，遵循已有约定；若无，询问用户偏好。写能复现 Bug 的失败测试，运行确认**失败**
2. **写修复（绿）** — 最小修复代码，运行确认复现测试**通过**
3. **运行全量测试** — 确认无回归
4. **补充回归测试（可选）** — 防止类似 Bug 复发
5. **更新 TASK.md** → 标记完成，自动继续下一个任务
